<!-- XML view definition for the website form -->
<template id="student_registration_form_template" name="Student Registration Form">
     <form t-name="student.student_registration_form">
        <div class="form-group">
            <label for="name">Name:</label>
            <input type="text" name="name" class="form-control" required="required"/>
        </div>
        <div class="form-group">
            <label for="phone">Phone:</label>
            <input type="tel" name="phone" class="form-control" required="required" pattern="[0-9]{10}"/>
            <small class="form-text text-muted">Please enter a 10-digit phone number.</small>
        </div>
        <div class="form-group">
            <label for="birth_date">Birth Date:</label>
            <input type="date" name="birth_date" class="form-control" required="required" max="2009-12-31" min="2018-01-01"/>
            <small class="form-text text-muted">Age must be between 4 and 5 years old.</small>
        </div>
        <div class="form-group">
            <label for="amount">Amount:</label>
            <input type="number" name="amount" class="form-control" required="required" step="0.01" min="0"/>
        </div>
        <!-- Add more fields as needed -->

        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</template>

<template id="registration_success_template" name="Registration Success">
    <div class="alert alert-success">
        Congratulations! Your registration has been successfully submitted.
    </div>
</template>